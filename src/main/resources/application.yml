server:
  port: 8000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/llm_ledger2?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

app:
  jwt:
    secret: super_secret_key_for_llm_ledger_change_in_production_super_secret_key_32
    expire-minutes: 10080
  ollama:
    api-url: http://localhost:11434/api/generate
    default-model: qwen3:4b
